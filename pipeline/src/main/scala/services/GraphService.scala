package services

import org.apache.spark.sql.SparkSession
import org.apache.spark.sql.DataFrame

class GraphService(spark : SparkSession) {
  def createCustomerNode(customerDf : DataFrame): Unit = {
    
  }
}
